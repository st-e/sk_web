<h1>Hauptflugbuch <%= h Settings.instance.location %></h1>

<p><%= h @date %></p>

<% if @flights.empty? %>
	<p>Keine Flüge</p>
<% else %>
	<p><%= h @flights.count %> Flüge</p>

	<%= render :partial => 'partials/list_table', :locals => { :table => @table } %>
<% end %>

