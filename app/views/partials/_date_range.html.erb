<%
	first=date_range.begin
	last=date_range.end
	last=last-1 if date_range.exclude_end?
%>
<% if first==last %>
	<%= h date_formatter(german_format, true).call(first) %>
<% else %>
	<%= h date_formatter(german_format, true).call(first) %> bis <%= h date_formatter(german_format, true).call(date_range.end) %>
<% end %>
